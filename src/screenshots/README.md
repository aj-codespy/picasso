# Drop your screenshots here

Put any `.png`, `.jpg`, `.jpeg`, or `.webp` files in this folder, then run:

```bash
picasso update
```

Only **new** images get analyzed (changed files are detected and re-analyzed;
renamed files keep their analysis). Images in this folder are never committed
to the repository.
